{
  "name": "vieta-hrm-monorepo",
  "version": "1.0.0",
  "description": "Viet A HRM Monorepo",
  "private": true,
  "scripts": {
    "build": "cd client && npm install --production=false && npm run build && cd ../server && npm install",
    "start": "node server/server.js"
  }
}
