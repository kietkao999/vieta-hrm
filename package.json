{
  "name": "vieta-hrm-monorepo",
  "version": "1.0.0",
  "description": "Viet A HRM Monorepo",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/server.js"
  }
}
